;; ---------------------------------------------------------------------------
;; PDLL bracket pairs (matching and auto-close)
;; ---------------------------------------------------------------------------

("(" @open ")" @close)
("[" @open "]" @close)
("{" @open "}" @close)
("<" @open ">" @close)
