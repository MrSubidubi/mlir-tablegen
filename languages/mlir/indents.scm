;; ---------------------------------------------------------------------------
;; MLIR indentation rules
;;
;; Regions are the primary indentation construct in MLIR: function bodies,
;; control-flow bodies, module bodies, and so on.
;; ---------------------------------------------------------------------------

(region "}" @end) @indent
